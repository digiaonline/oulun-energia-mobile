import 'package:flutter/material.dart';
import 'package:oulun_energia_mobile/views/utils/widget_ext.dart';

class InterruptionsSelectionsView extends StatelessWidget {
  static String routePath = '/interruptions';
  static String routeName = 'interruptions_selections_view';

  const InterruptionsSelectionsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('TODO Interruptions'))
        .withBackgroundColor(Colors.white);
  }
}
